#!/bin/fish

set -x SCRIPT_DIR $(echo "$HOME"/Storage/Projects/scripts)
set -x PROJECT_DIR $HOME/Storage/Projects

source "$SCRIPT_DIR/utils.fish"
source "$SCRIPT_DIR/youtube.fish"
source "$SCRIPT_DIR/quick.fish"
source "$SCRIPT_DIR/gom.fish"
source "$SCRIPT_DIR/alias.fish"
